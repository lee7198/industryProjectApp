import 'package:flutter/material.dart'
    show BuildContext, Colors, Scaffold, StatelessWidget, Text, Widget;

class AccessScreen extends StatelessWidget {
  const AccessScreen({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Scaffold(backgroundColor: Colors.white, body: Text('ERROR'));
  }
}
